class Solution {
    public int bulbSwitch(int n) {
        return (int) Math.sqrt(n);
    }
}

// Easy math solution:
// the factor of square number will be a odd count, which means it will be on at last.
